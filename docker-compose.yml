services:
  wieldos:
    build: .
    container_name: wieldos
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./plugins:/app/plugins
      - ./backups:/app/backups
